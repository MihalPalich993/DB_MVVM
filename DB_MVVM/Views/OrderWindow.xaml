<Window x:Class="DB_MVVM.Views.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оформление заказа" Height="300" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Window.Resources>
        <!-- Стиль для ошибок (красная рамка) -->
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={x:Static RelativeSource.Self},
                            Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Товары -->
        <TextBlock Text="Товары:" Grid.Row="0" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5"
                 Text="{Binding Products, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Сумма -->
        <TextBlock Text="Сумма (руб):" Grid.Row="1" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5"
                 Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>

        <!-- Доставка -->
        <TextBlock Text="Адрес доставки:" Grid.Row="2" VerticalAlignment="Center" Margin="0,5"/>
        <!-- Placeholder (подсказка) средствами простого текста -->
        <Grid Grid.Row="2" Grid.Column="1">
            <TextBox Name="AddressBox" Margin="0,5"
                     Text="{Binding DeliveryAddress, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Оставьте пустым для самовывоза" 
                       IsHitTestVisible="False" 
                       Foreground="Gray" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <!-- Показываем подсказку, если текст пустой -->
                            <DataTrigger Binding="{Binding Text, ElementName=AddressBox}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Width="100" Margin="5" Click="Accept_Click" IsDefault="True"/>
            <Button Content="Отмена" Width="100" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>